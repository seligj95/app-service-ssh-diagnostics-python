# Azure Developer CLI configuration file
# https://aka.ms/azure-dev-yaml

name: app-service-ssh-diagnostics-python
metadata:
  template: app-service-ssh-diagnostics-python@0.0.1-beta

services:
  web:
    project: .
    host: appservice
    language: python

infra:
  provider: bicep
